<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ESLTracker.Controls.Decks"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             xmlns:Enums="clr-namespace:ESLTracker.DataModel.Enums"
             xmlns:Controls="clr-namespace:ESLTracker.Controls" 
             xmlns:Converters="clr-namespace:ESLTracker.Utils.Converters"
             xmlns:ViewModels="clr-namespace:ESLTracker.ViewModels.Decks" 
             x:Class="ESLTracker.Controls.Decks.EditDeck"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="200">
    <UserControl.DataContext>
        <ViewModels:EditDeckViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <ObjectDataProvider x:Key="DeckTypeEnum" MethodName="GetValues"
                            ObjectType="{x:Type System:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="Enums:DeckType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ViewModels:CreateNewDeckCommand x:Key="CreateNewDeckCommand" />
        <Converters:CommandMultipleParamaters x:Key="CommandMultipleParamatersConverter" />
    </UserControl.Resources>
    <Grid>

        <Controls:DeckClassSelector x:Name="deckClass" HorizontalAlignment="Left" Margin="0,38,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="txtName" HorizontalAlignment="Left" Height="23" Margin="0,10,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"
                 Text="{Binding Deck.Name}"/>
        <Button x:Name="btnSave" Content="Button" HorizontalAlignment="Left" Margin="85,109,0,0" VerticalAlignment="Top" Width="75"
                Command="{StaticResource CreateNewDeckCommand}"
                >
            <Button.CommandParameter>
                <MultiBinding Converter="{StaticResource CommandMultipleParamatersConverter}">
                    <Binding Path=""/>
                    <Binding Path="DataContext" ElementName="deckClass"/>
                </MultiBinding>
            </Button.CommandParameter>
        </Button>
        <ComboBox x:Name="cbDeckType" 
                  ItemsSource="{Binding Source={StaticResource DeckTypeEnum}}" 
                  SelectedItem="{Binding Deck.Type}"
                  HorizontalAlignment="Left" Margin="10,73,0,0" VerticalAlignment="Top" Width="120"/>

    </Grid>
</UserControl>
